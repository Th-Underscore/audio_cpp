# user-specific persisted config (created by the Save button)
config.json
config.json.tmp

# runtime artifacts
outputs/
*.log
*.7z

__pycache__/
*.pyc

# per-message voice registry (runtime state, server-side source of truth)
audio_registry.json
